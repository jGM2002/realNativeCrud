import { StyleSheet } from 'react-native';

const fontStyle = StyleSheet.create({
    labelText: {
        color: 'red',
        fontSize: 16,
        marginBottom: 8,
    },
});

export default fontStyle;